/**
 * Content style hints for Android Auto browse tree items.
 * Used with `setBrowseTreeStyle` to control how items are displayed in Android Auto.
 */
export enum AndroidAutoContentStyle {
  Browsable = 1,
  List = 2,
  Grid = 3,
  CategoryGrid = 5,
  CategoryList = 6,
}
